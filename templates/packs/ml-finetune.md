# Pack — ML fine-tune (Soup, project-scoped)

Load when the job is a named fine-tune in an app repo. Soup is **CURATED_OPTIONAL**, not a global Orchestra skill.

- Do not add Soup to host-stack.
- Project-scoped install only. Secrets stay in the host.
- Papers and eval notes belong in that product's `idea.md`, not a second Brain catalog.

Skip this pack for ordinary frontend and college GUI.
