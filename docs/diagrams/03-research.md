# Research

```mermaid
flowchart TD
  miss[Graph miss or new URL]
  scrapling[Scrapling on a named public page]
  web[Web as fallback]
  one[Propose one named capability]
  wait[Wait for update]
  miss --> scrapling --> web --> one --> wait
```

Firecrawl on-demand. Crawl4AI catalog fallback. No LinkedIn / Instagram / ATS scrapers.
