# Combine sources

```mermaid
flowchart TD
  named[Named site or kit]
  extract[Extract language]
  one[One DESIGN.md]
  tint[Tint one token extend system]
  named --> extract --> one --> tint
```

Combine against the approved system. Not a collage. Not their logo or source.
