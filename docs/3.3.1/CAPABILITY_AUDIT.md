# 3.3.1 capability audit

Install none of these as core. Rows exist so the router can refuse them honestly.

| Candidate | Status | Runtime |
| --- | --- | --- |
| GSD archived (`gsd-archived`) | `REJECTED` | Do not install. Pattern already in Orchestra’s loop. See `gsd-core` `BOOKMARK`. |
| GSD Core (`gsd-core`) | `BOOKMARK` | Compare patterns only. |
| ECC (`affaan-ecc`) | `REJECTED` install; steal-thin | Absorb fresh-context review + focused-skill + verify loop only. |
| Karpathy skills (`karpathy-skills`) | `CATALOG` | **Install none** unless a single focused gap is proven later. |
| Brag (`brag`) | `PROJECT_ONLY` | Bound to `final-showcase`. |
| awesome-design-md | `ACTIVE` research_source | Strengthen DESIGN.md LOCKED vs OPEN. |
| PlayCanvas (`playcanvas`) | `EXPERIMENTAL` | `project_scoped_install`. Require CSS/DOM/Canvas/R3F cannot do this. |
| Desktop Commander MCP | `PRIVILEGED` | Explicit activation only. Not core. |
| modelcontextprotocol/servers | `CATALOG` | Never bulk-install. |
| Motion (`motion-dev`) | `PROJECT_ONLY` | One motion engine rule. |
| Skiper UI / ThreeUI | `REFERENCE` | One-component, not a kit dump. |
| HorizonX, Trickle, Motion Sites, Kexsio | `REFERENCE` | Research only. |
| Overleaf | `REFERENCE` | `research-paper` route only. |
| Firecrawl / Scrapling / Playwright | unchanged | Forensics vs our-app QA. |
| GetLayers | `PURCHASE_PENDING` | Unchanged. |

New routes (not new always-on skills as a dump): `visual-forensics`, `visual-golden-still`, `fresh-context-review`, `final-showcase`. Four thin skills load only when those routes fire.
