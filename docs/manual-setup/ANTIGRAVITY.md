# Antigravity — auth only

1. Open the **app repo**. Add Folder your private workspace (`ORCHESTRA_HOME`).
2. Paste `kit/antigravity/MASTER-PROMPT.md` once. Fill MODE, VAULT, APP ROOT.
3. **MCP Store:** Connect Playwright, Context7, Stitch. Sign in to GitHub if that job needs it.
4. Keep **science** and **data-agent-kit-plugin** off as Global. Re-enable only for that job.
5. Refero / Firecrawl / Serena: Connect when named. GetLayers: wait for purchase + **update**.
6. Do not install Gmail on Antigravity.

Skills land in `~/.gemini/config/skills` via `kit/sync-ides.ps1`.
