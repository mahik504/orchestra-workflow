# Claude Code — auth only

1. Global adapter: `~/.claude/CLAUDE.md` (copied from this repo's `CLAUDE.md` at bootstrap). A random college folder still reads that file unless it ships a competing `CLAUDE.md`.
2. Skills: `~/.claude/skills` after `kit/sync-ides.ps1`.
3. Add MCP in Claude Code's own config if you need GitHub, Refero, or Playwright there. Token in host env, never git.
4. GetLayers: do not `claude mcp add` until purchase.

Stand-down: type **skip orchestra** in the chat.
