# Versioning

| Version | Name | What it was |
| --- | --- | --- |
| **v1** | Workflow notes | Jump table, dual-track import, Cursor + Antigravity packets, public overlay without a capability matrix |
| **v2** | Capability router | Registries, protocols, activation card, originality gate, typography as a first-class protocol, init/install scripts, multi-agent adapters |

This repository is **Orchestra Workflow v2**.

v1 clones still work as documentation. v2 is additive: you do not need to rename your private notes. You do need the conductor skill + `protocols/` + `registries/` for the router to exist.

Flag: `VERSION` file at repo root = `2.0.0`.
