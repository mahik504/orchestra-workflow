---
title: Stack
---

# Stack

Catalog of **this** orchestra loop. Product facts do not belong in this public template.

| Remote | Visibility | Role |
| --- | --- | --- |
| Author’s vault remote | **private** | Full backup. 12h sync. Stay private. |
| [mahik504/orchestra-workflow](https://github.com/mahik504/orchestra-workflow) | **public** | This template. 12h publish when these files changed. Real commits only. |

## How the loop works

```
you fill templates/plan-brief.md
  → Cursor Plan (routes.md → that one idea.md, not the whole vault)
  → optional: npx skillui on ONE echo URL Plan named → projects/<slug>/ref/
  → Stitch = 2D screens / app chrome
  → 3D in code (R3F or Spline), not Stitch
  → Cursor Agent implements (React Bits only for earned web motion)
  → specialists only if Plan named them
  → ship-safe on our app; Strix when Docker is ready and the app is ours
```

| Step | Who | Notes |
| --- | --- | --- |
| Idea / architecture / “what next?” | **Cursor Plan** | Strongest thinking model. Brief required. |
| 2D screens / DESIGN.md | **Stitch** | Chrome only. One visual world per product. |
| Code | **Cursor Agent** | Match Stitch. Repo on disk beats vault. |
| It broke | **Cursor Debug** | |
| Extra polish / tests / CI | **Antigravity** | Packet in, summary out. |
| Library docs | **Context7** | |
| Click our running web UI | **Playwright** | |
| Security on **our** app | **ship-safe** always; **Strix** when we scan | |
| Research | **ChatGPT Go** | Packet only. |
| Long browse (until **2026-08-25**) | **Manus 1.6 Max** | Packet only. |
| Hostile review / deep think | **Claude.ai packet** or **Claude Code CLI** | CLI = separate terminal. |
| Android | **Expo** | Play Store first. |

If there is **no Cursor**, Antigravity is the conductor. Friend zip is only `kit/antigravity/` — never the private vault, never `mcp_config.json`.

## Skills installed (inspected 2026-08-18)

| Skill | Job |
| --- | --- |
| `orchestra-conductor` | Global loop unless “skip orchestra” |
| `orchestra-vault` | Lasting notes only; delete junk |
| `orchestra-ship` | GitHub + LinkedIn when showable |
| `orchestra-docs` | Slides / reports / papers |
| `ship-safe` | Defensive security on **our** apps |
| curated `expo-*` + `eas-app-stores` | Expo routing, layout, data, native UI, upgrade, stores |
| `stitch-*` + `taste-design` | 2D chrome and tokens |
| `impeccable` + Emil motion skills | Polish after a locked direction |
| Strix trio | Scan / patch / CI on **our** apps |

On-demand CLI, not a skill pack: `npx skillui` when Plan names **one** echo URL. That is reverse-engineering for tokens — not a clone factory.

No reminder MCP. Reminders live in the user rule and in chat.

## MCP (names only — no keys)

**Cursor** marketplace: GitHub, Playwright, Context7. Vault + Stitch MCP on the author’s machine.

Do not add Headroom MCP or extra hosts unless asked. Example without secrets: `kit/antigravity/mcp_config.example.json`.

## Product kinds (do not mix)

| kind | Meaning |
| --- | --- |
| `hiring-cv` | Tough, public, internship story |
| `personal` | Studio / parked |
| `college` | Assignments, due-now. Never the internship flagship |

Per-product stack lives in that `idea.md` on the **private** vault.

## Vault backup

| Piece | Status |
| --- | --- |
| 12h task | `OrchestraBrainVaultSync` → `kit/sync-both.ps1` |
| Public template | this repo, only when allowlisted files changed |
| Secrets | `.gitignore` blocks `mcp_config.json`, `.env`, tokens |
| Layer 2 (n8n / vectors / voice) | **not built** |

## Refused on purpose (do not install)

Second conductors: OpenHands, Dify, Langflow, Coolify, Maxun, OpenCode, Kilo, OmniRoute, 9router, gstack.

Skill dumps: `addyosmani/agent-skills`, vercel packs, obra/superpowers, frontend-design, ui-ux-pro-max.

Also skipped: Stirling-PDF, Crawl4AI, browser-use, code-review-graph MCP, fake GitHub activity generators, reminder MCP.
